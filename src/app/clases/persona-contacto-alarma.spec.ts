import { PersonaContactoAlarma } from './persona-contacto-alarma';

describe('PersonaContactoAlarma', () => {
  it('should create an instance', () => {
    expect(new PersonaContactoAlarma()).toBeTruthy();
  });
});
