import { DispositivosAuxiliaresTerminal } from './dispositivos-auxiliares-terminal';

describe('DispositivosAuxiliaresTerminal', () => {
  it('should create an instance', () => {
    expect(new DispositivosAuxiliaresTerminal()).toBeTruthy();
  });
});
